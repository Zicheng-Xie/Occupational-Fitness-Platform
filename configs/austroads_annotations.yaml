# IMPORTANT:
# These mappings should be completed only after a human has reviewed the
# relevant Austroads chapters/tables. Do not use an LLM to silently invent
# category/subcondition metadata.
#
# Matching is evaluated top-to-bottom. `set` values are attached to each chunk.
# At least one of section_regex/title_regex/text_regex should be present.
#
# Example SHAPE ONLY (not an Austroads factual mapping):
# - match:
#     section_regex: "^X\\.Y"
#   set:
#     module: "vision"
#     category: "vision"
#     subcondition: "visual_acuity"
#     licence_context: "commercial"

annotations: []
