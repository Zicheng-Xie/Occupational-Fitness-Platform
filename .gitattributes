# Evidence snapshots retain original formatting and are verified by file hashes.
outputs/demo/** -text -whitespace

# Preserve signed source notes and fingerprinted evaluation bundles byte-for-byte.
data/cases/**/*.txt -text -whitespace
outputs/evaluation/** -text -whitespace
